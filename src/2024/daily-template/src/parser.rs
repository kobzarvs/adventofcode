pub fn parse(input: &str) -> (todo!('return result')) {
    input.lines().enumerate().for_each(|(y, line)| {
        todo!('parse lines');
        line.chars().enumerate().for_each(|(x, c)| {
            let high = c.to_digit(10).unwrap() as i32;
        })
    });

    todo!('return result')
}
